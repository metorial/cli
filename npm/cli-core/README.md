# `@metorial/cli-core`

Shared runtime helpers for npm packages that launch the official Metorial CLI.

Most users should install [`@metorial/cli`](https://www.npmjs.com/package/@metorial/cli) instead. This package is for package authors and wrapper commands that want to delegate to the Metorial CLI from Node.js.

```bash
npm install -g @metorial/cli
```
